import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    section: {
        marginLeft: 20,
    },
    sectionHeader: {
        fontWeight: 'bold',
        fontSize: 24,
        paddingTop: 8
    }
})

export default styles;